# Hedera Hydropower MRV — Environment Variables
# Copy this file to .env and fill in your real values
# NEVER commit .env to git

# ========================================
# HEDERA CONFIGURATION
# ========================================

# Your Hedera testnet operator account
HEDERA_OPERATOR_ID=0.0.6255927

# ECDSA secp256k1 private key (DER hex format starting 3030...)
# Generate at: https://portal.hedera.com
HEDERA_OPERATOR_KEY=YOUR_PRIVATE_KEY_HERE

# HCS Topic ID for audit log
AUDIT_TOPIC_ID=0.0.7462776

# HREC Token ID (Hedera Token Service)
REC_TOKEN_ID=0.0.7964264

# Grid emission factor (tCO2/MWh) — India CEA default
EF_GRID=0.8

# ========================================
# API AUTHENTICATION (NEW)
# ========================================

# Comma-separated list of valid API keys
# Format: ghpk_<org_id>_<random_hex>
# Generate using: node -e "console.log(require('./src/middleware/auth').generateAPIKey('ORG001'))"
VALID_API_KEYS=ghpk_pilot_plant_001,ghpk_demo_plant_002,ghpk_test_plant_003

# Node environment (development/production)
# In development mode, API auth is bypassed if no keys configured
NODE_ENV=development

# ========================================
# SECURITY BEST PRACTICES
# ========================================
# 1. Store API keys in secure vault (AWS Secrets Manager, HashiCorp Vault)
# 2. Rotate keys quarterly
# 3. Use database with hashed keys in production (see src/middleware/auth.js)
# 4. Enable rate limiting in production
# 5. Use HTTPS/TLS for all API traffic
# 6. Never log or expose API keys in responses