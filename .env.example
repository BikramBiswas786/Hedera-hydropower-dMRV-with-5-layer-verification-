# ============================================================
# Hedera Hydropower dMRV — Environment Variables
# Copy this file to .env and fill in real testnet values.
# ============================================================

# ── Hedera Operator (Testnet) ──────────────────────────────
# Use EITHER name — both are accepted by the code:
HEDERA_OPERATOR_ID=0.0.6255927          # PowerShell / legacy name
HEDERA_ACCOUNT_ID=0.0.6255927           # Vercel standard name

# Use EITHER name — both are accepted:
HEDERA_OPERATOR_KEY=3030020100300706052b8104000a04220420...   # PowerShell / legacy
HEDERA_PRIVATE_KEY=3030020100300706052b8104000a04220420...    # Vercel standard

# ── HCS Topic (audit trail) ────────────────────────────────
# Use EITHER name — both are accepted:
AUDIT_TOPIC_ID=0.0.7462776              # Vercel standard
HEDERA_TOPIC_ID=0.0.7462776            # PowerShell legacy name

# ── HTS Token (HREC carbon credits) ───────────────────────
CARBON_TOKEN_ID=0.0.7964264
TREASURY_PRIVATE_KEY=3030020100300706052b8104000a04220420...  # same as HEDERA_PRIVATE_KEY for demo

# ── Carbon Calculation ─────────────────────────────────────
# Use EITHER name — both are accepted:
EF_GRID=0.82                            # Vercel standard (tCO2/MWh, ACM0002)
GRIDEF=0.82                             # PowerShell legacy name

# ── API Auth ───────────────────────────────────────────────
API_KEY=demo_key_001

# ── App ────────────────────────────────────────────────────
# Set to 'production' on Vercel to disable mock fallbacks
NODE_ENV=production
PORT=3000
